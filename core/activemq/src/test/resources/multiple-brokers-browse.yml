jms:
  activemq:
    brokers:
      - name: broker-one
        url: tcp://localhost:11111
        queue: ActiveMQ.DLQ
        readOnly: true
        read:
          frequency: 1
          timeUnit: SECOND
      - name: broker-two
        url: tcp://localhost:22222
        queue: ActiveMQ.DLQ
        readOnly: true
        read:
          frequency: 10
          timeUnit: SECOND
